/* Netlify _redirects file - SPA routing configuration
   This is a backup to netlify.toml SPA redirects
   Ensures all routes serve /index.html for React Router
*/

/* SPA routing - serve index.html for all non-file routes */
/*  /index.html  200

/* API proxy configuration (optional)
   If you want Netlify Functions or serverless redirects in the future
   /api/*  https://ravi-maligai.onrender.com/api/:splat  200
